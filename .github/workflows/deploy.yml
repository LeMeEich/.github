name: Deploy

on:
  workflow_call:
    inputs:
      run_id:
        description: 'Run ID do build'
        required: true
        type: string
      environment:
        description: 'Ambiente para deploy'
        required: true
        type: string

jobs:
  deploy:
    name: Deploy Build #${{ inputs.run_id }} to ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate Branch
        uses: LeMeEich/.github/.github/actions/Deploy/validate-branch@master

      - name: Get Build Information
        id: build_info
        uses: LeMeEich/.github/.github/actions/Deploy/get-build-info@master
        with:
          run_id: ${{ inputs.run_id }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ steps.build_info.outputs.artifact_name }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ inputs.run_id }}

      - name: Deploy Application
        uses: LeMeEich/.github/.github/actions/Deploy/script@master
        with:
          environment: ${{ inputs.environment }}

      - name: Deployment summary
        run: |
          echo "### 🚀 Deploy Completed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: ${{ inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Number**: [#${{ steps.build_info.outputs.build_number }}](https://github.com/${{ github.repository }}/actions/runs/${{ inputs.run_id }})" >> $GITHUB_STEP_SUMMARY
          echo "- **Run ID**: ${{ inputs.run_id }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Artifact**: ${{ steps.build_info.outputs.artifact_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Deployment successful!" >> $GITHUB_STEP_SUMMARY